import 'package:flutter/material.dart';

class UserControlPage extends StatelessWidget {
  const UserControlPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Control de Usuarios'),
    );
  }
}