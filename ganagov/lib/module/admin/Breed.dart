import 'package:flutter/material.dart';

class BreedCrudPage extends StatelessWidget {
  const BreedCrudPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CRUD de Razas'),
    );
  }
}